# Pinned deliberately: a major pandas release is not
# something to discover on a scheduled run. Widen on purpose, never by accident.
yfinance>=1.2,<1.3
pandas>=2.2,<3.0
tzdata>=2024.1          # so zoneinfo("Europe/London") resolves on any runner

# Pillow is only needed by make_icons.py, run by hand; the PNGs are committed.
